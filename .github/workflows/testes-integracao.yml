# Action para linter e testes de Integração
name: Testes de Integração
on:
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v5
